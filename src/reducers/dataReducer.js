
export const dataReducer = (state, {type, payload}) => {
    switch (type) {
        case 'ADD_DATA':
            return [
                {}
            ]
    
        default:
            break;
    }
}